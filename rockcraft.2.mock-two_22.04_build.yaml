name: mock-two  # a folder with this name must exist at the project's root
version: 1.0.1
base: 22.04     # Dockerfile.22.04 must exist in the above folder
platforms:
  amd64:
  arm:
    build-on: [amd64]
  arm64:
    build-on: [amd64]
  ppc64le:
    build-on: [amd64]
  s390x:
    build-on: [amd64]
